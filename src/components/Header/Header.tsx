import React from "react";
import Navbar from "../Navbar/Navbar";
import "./Header.css";

const Header: React.FC = () => {
  return (
    <header>
      <Navbar />
    </header>
  );
};

export default Header;
