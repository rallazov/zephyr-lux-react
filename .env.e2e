# Loaded by `vite build --mode e2e` so Playwright (vite preview) calls a real API origin.
# Playwright starts `npm run api:dev` on this port before building/previewing the storefront.
VITE_PUBLIC_API_URL=http://127.0.0.1:3333
